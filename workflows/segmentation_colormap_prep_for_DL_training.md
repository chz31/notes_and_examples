This is a workflow for generating a uniform labelmap for all segmentations using colormap for deep learning model training.

1. First, create a label map node

2. Change segment names to remove "_"

3. Import color.txt into Slicer to assign the same color map to each segmentation.

